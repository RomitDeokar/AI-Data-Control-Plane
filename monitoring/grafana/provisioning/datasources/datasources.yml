apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

  - name: ControlPlane-Postgres
    type: postgres
    access: proxy
    url: postgres:5432
    database: kestra
    user: kestra
    secureJsonData:
      password: k3str4
    jsonData:
      sslmode: disable
      postgresVersion: 1600
